import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'comunidad.app',
  appName: 'Comunidad_Alerta_1.0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
